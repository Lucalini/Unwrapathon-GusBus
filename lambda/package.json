{
  "name": "processing-lambda",
  "version": "1.0.0",
  "description": "Lambda function to process widget schema and store in DynamoDB",
  "main": "processing-lambda.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.490.0",
    "@aws-sdk/client-bedrock-runtime": "^3.490.0",
    "@aws-sdk/util-dynamodb": "^3.490.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
